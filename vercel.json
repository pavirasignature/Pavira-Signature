{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "npm install --include=dev --legacy-peer-deps",
  "buildCommand": "npm run build -w frontend && npm install --legacy-peer-deps -w backend",
  "outputDirectory": "frontend/.next",
  "regions": ["bom1"],
  "cleanUrls": true,
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        }
      ]
    }
  ]
}
