# Workspace instructions

- When the user asks for code changes or fixes, implement the requested changes and then commit and push them to GitHub unless the user explicitly says not to.
- Use the repository's existing Git remote and main branch by default.
- Keep commit messages concise and descriptive.
- If no actual file changes were made, do not create a commit or push.
